<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>영화 리스트</h1>
    <table border='1'>
      <tr>
        <th>이름</th>
        <th>제목</th>
        <th>역할</th>
      </tr>
      <% movie.forEach(actor => { %>
      <tr>
        <td><%= actor.userName %></td>

        <% let movie_len = actor.careers.length %>

        <td> <% for(let i=0;i<movie_len;i++){ %>
          <% if(actor.careers[i].categor === "movie"){ %>
            <%= actor.careers[i].title %> <br>
            <% } %>
            <% } %>
        </td>
       <td> <% for(let i=0;i<movie_len;i++){ %>
          <% if(actor.careers[i].categor === "movie"){ %>
            <%= actor.careers[i].role %> <br>
            <% } %>
            <% } %>
        </td>
      </tr>
      <% }) %>
    </table>
    <a href="/actors">홈으로</a>
  </body>
</html>
